@using CodingEvents.Models

@model List<CodingEvents.Models.EventCategory>

<h1>Hi there Event Category</h1>

<table>
    <tr>
        <th>Category Name</th>
    </tr>
    @foreach(EventCategory category in Model)
    {
        <tr>
            <td>@category.Name</td>
        </tr>
    }
</table>
